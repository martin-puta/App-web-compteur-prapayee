import { atom } from 'recoil';

export const itemMenuSelected = atom({
  key: 'itemMenuSelected',
  default: 0,
});

export const StateOfMobuleMenu = atom({
  key: 'StateOfMobuleMenu',
  default: false,
});
